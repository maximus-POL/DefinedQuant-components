# {{TITLE}}

`dq.{{CATEGORY}}.{{SLUG}}` is a scaffold. Replace every `TODO` before requesting review.

## Intent

TODO: state the single financial question this component answers.

## When to use it

TODO: summarize when this calculation is appropriate.

## Formula

`TODO: provide the exact formula executed by this component.` Define every symbol.

## Inputs

The canonical input fields, units, and defaults are defined by `Inputs` in
`component.py`. Explain their financial meaning here without redefining them.

## Output

The canonical output fields and units are defined by `Output` in `component.py`.
Explain how a user should interpret them.

## Semantic ports

TODO: attach closed `semantic_port_metadata` to at least one canonical input and output field,
then explain which producer/consumer meanings are compatible. Reuse or deliberately extend the
protocol vocabulary; never invent component-local schema-extra keys.

## Worked example

TODO: add a small, synthetic, hand-checkable example.

## Assumptions and limitations

TODO: explain the important assumptions, unsupported scope, and likely mistakes.

## Evidence

`evidence.yaml` binds numerical claims to named tests in `test_component.py`.
Replace the scaffold with synthetic, deterministic evidence before publishing.
