interface:
  display_name: "Defined Quant Discovery"
  short_description: "Discover, compose, run, and render quant components"
  default_prompt: "Use $use-defined-quant to discover and compare supported components for my financial task, resolve required inputs, and execute the best fit."
